# Requirements Archive: TimeTracker v1.1 Hardening

**Defined:** 2026-02-28
**Shipped:** 2026-02-28
**Status:** All 22 requirements verified and complete

## Security

- [x] **SEC-01**: Hook scripts sanitize SESSION_ID to prevent shell injection (S1)
- [x] **SEC-02**: Hook scripts sanitize SESSION_ID to prevent path traversal (S2)
- [x] **SEC-03**: Undo snapshot deserialization validated with Zod schema (S3)
- [x] **SEC-04**: Pulse rate limit and session singleton wrapped in transaction (S4)
- [x] **SEC-05**: Database and config files created with restrictive permissions (700/600) (S5)
- [x] **SEC-06**: Note content capped at 10,000 characters (S6)
- [x] **SEC-07**: Git commands use `--` separator to prevent hash-as-flag injection (S7)
- [x] **SEC-08**: Pulse errors logged to `~/.tt/pulse-errors.log` instead of silently swallowed (S8)

## Correctness

- [x] **COR-01**: `getStartOfToday()` uses Luxon `DateTime.now().startOf('day')` instead of raw `new Date()` (C1)
- [x] **COR-02**: Dead `findBySlug('')` call removed from stop(), replaced with `findById()` (C2)
- [x] **COR-03**: N+1 `findAll().find()` calls replaced with `findById()` on project repository (C3)
- [x] **COR-04**: Migration error handler only catches "already exists" errors, not all errors (C4)
- [x] **COR-05**: Dead TTY branch removed from `resolveProject()` (C5)
- [x] **COR-06**: LIKE wildcards escaped in session prefix search (C6)

## Performance

- [x] **PERF-01**: Config loaded once at service creation, not on every pulse (P1)
- [x] **PERF-02**: Git root cached per working directory, not spawned on every pulse (P2)

## Code Quality

- [x] **QUAL-01**: `computeSessionDuration()` extracted to shared `duration.ts` utility (Q1)
- [x] **QUAL-02**: Active session resolution pattern extracted to `resolveActiveSession()` helper (Q2)
- [x] **QUAL-03**: `session-service.ts` split into focused services (pulse, lifecycle, edit) under 800 lines each (Q3)
- [x] **QUAL-04**: `handleCommandError` refactored to use base `TimeTrackerError` class (Q4)
- [x] **QUAL-05**: `withTransaction` signature simplified to `fn: () => T` (Q6)
- [x] **QUAL-06**: Compound index added on `activity_pulses(terminal_id, timestamp)` (Q7)

## Testing

- [x] **TEST-01**: Unit test infrastructure with vitest and mocked repositories
- [x] **TEST-02**: Integration test infrastructure with `bun test` for DB layer
- [x] **TEST-03**: Core session lifecycle tests (start, stop, pulse, idle detection)
- [x] **TEST-04**: Reporting engine tests (today, week, log aggregation)
- [x] **TEST-05**: Session correction tests (edit, undo, split, merge)
- [x] **TEST-06**: 80% coverage on core logic — achieved 96% statements, 85% branches

## CLI Commands

- [x] **CLI-01**: `tt alias add <dir> <slug>` command for project alias management (PROJ-02)
- [x] **CLI-02**: `tt rate set <project> <rate>` command for hourly rate management (PROJ-03)

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| SEC-01..08 | Phase 6 | Complete |
| COR-01..06 | Phase 7 | Complete |
| PERF-01..02 | Phase 7 | Complete |
| QUAL-01..06 | Phase 8 | Complete |
| TEST-01..06 | Phase 9 | Complete |
| CLI-01..02 | Phase 10 | Complete |

---
*Archived: 2026-02-28*
